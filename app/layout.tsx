import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Noghte Vorood - پادکست نقطه‌ورود | Technology & Culture Podcast",
  description: "NoghteVoroord (meaning 'Entry Point') is where deep conversations begin — about technology, society, creativity, and the future. Each episode features honest, thought-provoking discussions with guests from fields like startups, AI, design, marketing, and culture.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
